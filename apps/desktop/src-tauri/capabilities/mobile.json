{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "mobile",
  "description": "mobile-only permissions — the barcode scanner plugin is iOS/Android-only (P2.3)",
  "platforms": ["iOS", "android"],
  "windows": ["main"],
  "permissions": ["barcode-scanner:default"]
}
