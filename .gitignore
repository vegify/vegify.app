.DS_Store
node_modules/
.env
.env.*
!.env.example

# build output
.next/
.output/
.nitro/
.tanstack/
.turbo/
dist/
*.tsbuildinfo

# Rust workspace build output (one shared target dir at the repo root)
/target/

# Server-stack deploy artifacts (CI builds these; a placeholder is fine for local synth)
infra/assets/

# local sqlite
.data/

# Design binaries live in Figma cloud + Dropbox, not git
*.sketch
*.fig
*.psd

CLAUDE.md
CLAUDE.local.md
.claude/
